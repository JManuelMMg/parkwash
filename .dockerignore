# Ignorar el entorno virtual (venv) y archivos de caché de Python.
venv/
__pycache__/
*.py[cod]

# Ignorar archivos temporales, logs, y archivos de entorno (por ejemplo, .env si se monta en docker-compose).
*.log
*.swp
.env

# Ignorar archivos de IDE (por ejemplo, .vscode, .idea, etc.).
.vscode/
.idea/

# Ignorar archivos de git (opcional, si no se desea copiar el historial de git).
.git/
.gitignore

# Ignorar archivos de Docker (opcional, si se copia todo el contexto).
Dockerfile
docker-compose.yml
.dockerignore

# Ignorar archivos de recolección de estáticos (por ejemplo, staticfiles) si se montan como volumen en docker-compose.
# (Comentado, ya que se monta en docker-compose.)
# staticfiles/

# Ignorar archivos de media (por ejemplo, media) si se montan como volumen en docker-compose.
# (Comentado, ya que se monta en docker-compose.)
# media/

# Ignorar la base de datos (por ejemplo, db.sqlite3) si se monta como volumen en docker-compose.
# (Comentado, ya que se monta en docker-compose.)
# db.sqlite3 